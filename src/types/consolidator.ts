export type Consolidator = {
	id: string;
	name: string;
};

export type ConsolidatorSelectOption = {
	value: string;
	label: string;
};

export type ConsolidatorUser = {
	user_id: string;
	name?: string;
};
